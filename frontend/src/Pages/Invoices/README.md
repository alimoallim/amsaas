# Invoices UI

| Page | Status |
|------|--------|
| `InvoicesIndex.vue` | Active — billing operations (authenticated `api` client) |
| `BulkInvoiceManager.vue` | Quarantined — stub; no API calls (C7) |
| `BatchInvoiceDashboard.vue` | Quarantined — stub; no API calls (C7) |

Re-enable bulk UI after Sprint 1 (canonical invoice model + bulk API).
